<%= form_with(model: difficulty) do |form| %>
  <% if difficulty.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(difficulty.errors.count, "error") %> prohibited this difficulty from being saved:</h2>

      <ul>
        <% difficulty.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
